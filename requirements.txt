# Used by Vercel's Python runtime (Render/Docker use pyproject.toml). Keep in sync with pyproject dependencies.
sqlalchemy>=2.0
pydantic>=2.0
fastapi>=0.110
pandas>=2.0
openpyxl>=3.1
python-multipart>=0.0.9
psycopg[binary]>=3.1
